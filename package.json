{
  "name": "test_project",
  "version": "1.0.0",
  "dependencies": {
    "@sap/cds": "^9"
  },
  "devDependencies": {
    "@cap-js/sqlite": "^2"
  },
  "scripts": {
    "start": "cds watch",
    "build": "cds build",
    "deploy": "cds deploy --to sqlite:dev.db"
  },
  "private": true
}
